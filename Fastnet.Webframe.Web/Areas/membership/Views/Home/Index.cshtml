@model Fastnet.Webframe.Web.Areas.membership.Models.MembershipModel
@{
    ViewBag.Title = "Membership";
}
@*<div class="row">
    <div class="membership-heading">
        Membership
    </div>
</div>
<div class="row">

</div>*@
@section scripts {
    @Scripts.Render("~/bundles/membership")
    <script>
        $(function () {
            $.fastnet$utilities.AddOptions(@Html.Raw(@Model.Options.ToJson()));
            var membership = new $.webframe$membership();
            membership.start(@Html.Raw(@Model.ToJson()));
        });
    </script>
}
